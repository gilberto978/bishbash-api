# BishBash Broker Check API

A tiny backend that checks brokers using Bing Search API.

## Usage
Endpoint: `/api/checkBroker?broker=24option`

- Returns a JSON object with broker verdict.
- Powered by Azure Bing Search API.
